{
  "name": "@gorilly/website-sendmail",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "tar -a -c -f function.zip ./* && aws lambda update-function-code --function-name send-mail --zip-file fileb://function.zip"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@sendgrid/mail": "^7.2.4"
  }
}
