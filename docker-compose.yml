version: "3"
services:
        web:
                image: nginx
                volumes:
                        - ${PWD}/src:/usr/share/nginx/html
                        - ${PWD}/nginx_conf:/etc/nginx/conf.d
                ports:
                        - "80:80"
